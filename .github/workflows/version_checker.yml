name: Update Minecraft Version

on:
  schedule:
    - cron: "0 0 * * *"  # Schedule the workflow to run daily

jobs:
  update_version:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Get Latest Minecraft Version
        id: get_version
        run: |
          response=$(curl -s https://launchermeta.mojang.com/mc/game/version_manifest.json)
          latest_version=$(echo "$response" | jq -r .latest.release)
          echo "::set-output name=latest_version::$latest_version"

      - name: Update stored_version.txt
        run: |
          echo "${{ steps.get_version.outputs.latest_version }}" > stored_version.txt
          git config --local user.email "actions@github.com"
          git fetch origin main  # Fetch the latest changes from the main branch
          git rebase origin/main  # Rebase the changes on top of the latest main branch
          git pull
          git push origin main  # Push the updated changes to the main branch

